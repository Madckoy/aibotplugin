filters:
  - "abs(x - cx) <= r"
  - "abs(y - cy) <= r"
  - "abs(z - cz) <= r"
  - "!(x == cx && z == cz)"  # исключаем центральную колонну
sort:
  - "-(y - cy)"  # сверху вниз
  - "abs(x - cx) + abs(z - cz)"  # от центра к краям