filters:
  - "Math.abs(x - cx) + Math.abs(z - cz) <= (cy - y)"
  - "y <= cy"
  - "!(x == cx && z == cz)" # исключаем центральный столб
sort:
  - "(cy - y)"              # сначала верхние слои
  - "Math.sqrt((x-cx)^2 + (z-cz)^2)"  # от центра к краям
